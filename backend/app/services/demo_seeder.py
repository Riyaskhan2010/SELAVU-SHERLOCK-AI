"""
Demo seeder — REMOVED.

Demo data functionality has been replaced by the 6-source Data Sources system.
New users start with zero data. There is no pre-seeded demo account.

This file is kept as a no-op stub to avoid import errors if anything
references it during the transition period.
"""


async def run_demo_seeder(db) -> None:  # type: ignore[type-arg]
    """No-op. Demo seeding is disabled."""
    pass
